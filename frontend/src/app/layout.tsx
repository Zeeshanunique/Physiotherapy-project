import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PhysioTracker - AI-Powered Exercise Monitoring',
  description: 'Real-time physiotherapy exercise tracking and monitoring using AI pose detection',
  keywords: 'physiotherapy, exercise, AI, pose detection, rehabilitation, health tracking',
  authors: [{ name: 'PhysioTracker Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  )
}
